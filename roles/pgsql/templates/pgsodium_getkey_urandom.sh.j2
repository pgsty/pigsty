
#!/bin/bash

PG_DATA_DIR="{{ pg_data }}"
KEY_FILE="$PG_DATA_DIR/pgsodium_root.key"

if [ ! -f "$KEY_FILE" ]; then
    head -c 32 /dev/urandom | od -A n -t x1 | tr -d ' \n' > $KEY_FILE
fi
cat $KEY_FILE
